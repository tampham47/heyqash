{
  "name": "heyapis",
  "version": "0.1.0",
  "description": "heyapis",
  "main": "handler.js",
  "scripts": {
    "deploy": "serverless deploy",
    "deploy:prod": "serverless deploy --stage prod",
    "deploy:all": "yarn run deploy && yarn run deploy:prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tampham47",
  "license": "ISC",
  "dependencies": {
    "@heyliquid/shared": "^0.1.0"
  }
}
