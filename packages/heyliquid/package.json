{
  "name": "heyliquid",
  "version": "0.1.0",
  "description": "hey liquid",
  "main": "handler.js",
  "scripts": {
    "deploy": "serverless deploy",
    "deploy:prod": "serverless deploy --stage prod",
    "deploy:all": "yarn run deploy && yarn run deploy:prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@slack/web-api": "^5.9.0",
    "firebase-admin": "^8.12.1"
  },
  "author": "tampham47",
  "license": "ISC"
}
